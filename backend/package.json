{
  "name": "backend",
  "version": "1.0.0",
  "description": "API de clientes - sem dependencias externas (usa apenas modulos nativos do Node)",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=22"
  }
}
